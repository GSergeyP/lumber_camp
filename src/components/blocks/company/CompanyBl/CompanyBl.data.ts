import type { TTags } from '@modifiers/TagsMo.type';

const data: TTags = {
  tags: 'article',
  position: 'relative',
  margin: '0 auto',
  width: {
    xl: '1440px',
    lg: '100%',
    md: '100%',
    sm: '100%',
    xs: '100%'
  }
}

export { data }