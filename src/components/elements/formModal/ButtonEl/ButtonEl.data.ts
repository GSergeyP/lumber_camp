import type { TTags } from '@modifiers/TagsMo.type';

const data: TTags = {
  tags: 'button',
  variant: 'eightfold',
  position: 'absolute',
  top: '10px',
  right: '10px',
  click: true,
  icon: {
    title: 'Cross',
    width: '40px',
    height: '40px',
    classes: 'label-icon'
  }
}

export { data }